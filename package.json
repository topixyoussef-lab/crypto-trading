{
  "name": "crypto-trading-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "server": "cd server && node index.js",
    "client": "cd client && npx vite --host",
    "start": "start cmd /c \"cd server && node index.js\" && start cmd /c \"cd client && npx vite --host\""
  }
}
